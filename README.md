## Drag & Drop Design Kit

Project is structured using React + TypeScript + Vite 

## Scripts

- Run `npm i` from `assets` folder to install required npm packages.
- Run `npm run dev` for development
- Run `npm run build` for production.
